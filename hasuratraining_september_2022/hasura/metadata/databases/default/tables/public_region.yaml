table:
  name: region
  schema: public
array_relationships:
  - name: orders
    using:
      foreign_key_constraint_on:
        column: region
        table:
          name: order
          schema: public
remote_relationships:
  - definition:
      hasura_fields:
        - value
      remote_field:
        region_info:
          arguments:
            where:
              name:
                _eq: $value
      remote_schema: aux
    name: info
